# HTML 模板

新建 domain 方向时，从标杆复制：

```bash
cp -r ../paleontology/templates/timemachine.{html,css,js} .
# 或整目录： cp -r ../../paleontology/templates .
```

构建：`bash skills/book-learn-distill/scripts/build-timemachine-html.sh`（当前默认 paleontology；扩展脚本时传入 slug）。
